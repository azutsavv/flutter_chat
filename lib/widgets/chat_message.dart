import 'package:flutter/material.dart';

class ChatMeassage extends StatelessWidget {
  const ChatMeassage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("no new messages"),);
  }
}